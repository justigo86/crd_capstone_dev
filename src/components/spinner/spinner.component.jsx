import "./spinner.styles.scss";

//used in categories.selector, category.compenent, and categories-preview.component
const Spinner = () => (
  <div className="spinner-overlay">
    <div className="spinner-container"></div>
  </div>
);

export default Spinner;
